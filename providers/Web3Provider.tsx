"use client";

/**
 * Base (EVM) wallet layer — RainbowKit + wagmi + TanStack Query.
 * Re-exports the app shell provider; see `components/web3-providers-inner.tsx`.
 */
export { Web3ProvidersInner as Web3Provider } from "@/components/web3-providers-inner";
